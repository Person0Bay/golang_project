## Overcooked Review System — микросервисная система отзывов

Система отзывов на блюда с микросервисной архитектурой.

### 🏗️ Архитектура

Проект разделён на несколько сервисов:

- **dish-svc** (порт 8081) — сервис блюд
  - CRUD для ресторанов и блюд
  - генерация QR-кодов
  - управление меню

- **rate-svc** (порт 8082) — сервис оценок
  - приём оценок и отзывов
  - валидация заказов по QR-кодам чеков
  - контроль дублей отзывов
  - публикация событий в Kafka

- **agg-svc** — сервис агрегации
  - консьюмер событий из Kafka
  - пересчёт среднего рейтинга
  - обновление агрегированной аналитики в Redis

- **analytics-svc** (порт 8083) — сервис аналитики
  - работа по данным из Redis
  - самое популярное блюдо за день
  - лучшее блюдо за всё время

- **api-gateway** (порт 8080) — API Gateway
  - единая точка входа для фронтенда
  - маршрутизация запросов к нужным сервисам

Фронтенд — это набор простых HTML/JS страниц, которые ходят в `api-gateway`.

### 🆕 Ключевые возможности

- **Масштабирование по ресторанам** (`restaurant_id`)
- **QR-коды от чеков** — отзыв можно оставить только на реально заказанное блюдо
- **Redis-аналитика**:
  - лучшее блюдо в ресторане по оценкам за день
  - лучшее блюдо по оценкам за всё время
- **Микросервисная архитектура**
- **Контроль дублей отзывов** через Redis
- **Kafka** для асинхронной обработки

### 🚀 Быстрый старт (актуально для текущего репо)

Основной сценарий сейчас ориентирован на **Windows + Docker Desktop**.

1. Установите и запустите Docker Desktop
2. В корне репозитория выполните `start.bat` (двойной клик или через терминал)
3. После успешного запуска откройте в браузере: `http://localhost`

Файл `docker-compose.yml` поднимает все сервисы, базу, кэш и остальные зависимости.


## 📡 API эндпоинты

### Dish Service (8081)
- `POST /api/restaurants` - Создать ресторан
- `POST /api/restaurants/{id}/dishes` - Создать блюдо
- `GET /api/restaurants/{id}/dishes` - Получить блюда ресторана
- `GET /api/restaurants/{restaurantId}/dishes/{dishId}` - Получить конкретное блюдо

### Rate Service (8082)
- `POST /api/restaurants/{restaurantId}/dishes/{dishId}/reviews` - Создать отзыв
- `GET /api/restaurants/{restaurantId}/dishes/{dishId}/reviews` - Получить отзывы

### Analytics Service (8083)
- `GET /api/restaurants/{restaurantId}/analytics` - Получить аналитику
- `GET /api/restaurants/{restaurantId}/dishes/{dishId}/stats` - Статистика блюда
- `GET /api/restaurants/{restaurantId}/top-dishes` - Топ блюд

## 🏪 Поддержка множества ресторанов

Каждый запрос должен содержать `restaurant_id` для масштабирования:
- Блюда привязаны к ресторанам
- Отзывы привязаны к ресторанам и заказам
- Аналитика ведется по ресторанам

## 🔒 Валидация QR-кодов (чеков)

Система проверяет, что:
1. Блюдо действительно заказывалось в этом заказе
2. Отзыв можно оставить только один раз на блюдо в заказе
3. Блюдо принадлежит указанному ресторану

## 📊 Redis-аналитика

Сервис собирает и предоставляет:
- **Самое лучшее блюдо за день** - средний рейтинг
- **Самое лучшее блюдо за все время** - по среднему рейтингу
- **Топ-10 блюд** по рейтингу

## 🛠️ Технологический стек

- **Backend:** Go (Golang)
- **Database:** PostgreSQL
- **Cache:** Redis
- **Message Queue:** Kafka
- **API Gateway:** Custom Go service
- **Frontend:** HTML, CSS, JavaScript
- **Containerization:** Docker

## 📁 Структура проекта (фактическая)

Корень репозитория:

```text
overcooked-simplified_final/
├── agg-svc/              # Сервис агрегации (Go-сервис + Dockerfile)
├── analytics-svc/        # Сервис аналитики (Go-сервис + Dockerfile)
├── api-gateway/          # API Gateway (Go-сервис + Dockerfile)
├── dish-svc/             # Сервис блюд (Go-сервис + Dockerfile)
├── rate-svc/             # Сервис оценок (Go-сервис + Dockerfile)
├── database/             # SQL-скрипты для инициализации БД
├── frontend/             # Фронтенд (HTML/JS страницы для админки, аналитики и отзывов)
├── uploads/              # Загруженные картинки блюд/ресторанов (используются фронтендом)
├── docker-compose.yml    # Оркестрация сервисов и инфраструктуры
├── nginx.conf            # Конфигурация Nginx для фронтенда и прокси
├── start.bat             # Запуск всех контейнеров под Windows
├── stop.bat              # Остановка контейнеров под Windows
├── test-services.bat     # Быстрый health-check поднятых сервисов
└── README.md
```


## 🔧 Локальная разработка (без Docker)

Для локальной разработки без Docker:

1. Установите Go 1.21+
2. Установите PostgreSQL, Redis, Kafka
3. Запустите каждый сервис отдельно (из соответствующей папки `*_svc` / `api-gateway`)
4. Настройте переменные окружения так же, как это сделано в `docker-compose.yml`

## ✅ Тестирование

В проекте есть как юнит-тесты у сервисов, так и интеграционный тест в отдельной папке.

- **Все тесты сразу (из корня репо):**

```bash
go test ./...
```

- **Только интеграционные тесты:**

```bash
cd tests
go test -v
```

- **Проверка, что сервисы живы (Windows):**

После того как вы подняли всё через `start.bat`, можно выполнить:

```bat
test-services.bat
```

Скрипт пройдётся по health-эндпоинтам API Gateway, сервисов и фронтенда и выведет результат в консоль.

## 🐛 Устранение неполадок

1. **Ошибка порта:** Убедитесь, что порты 80, 8080-8083, 5444, 6379, 9092 свободны
2. **Kafka не запускается:** Увеличьте время ожидания в start.sh/start.bat
3. **PostgreSQL ошибки:** Убедитесь, что данные удалены: `docker volume prune`
4. **Сервисы не видят друг друга:** Проверьте Docker network

## 📝 Примеры использования

### Создание ресторана и блюда:
```bash
# Создать ресторан
curl -X POST http://localhost/api/restaurants \
  -H "Content-Type: application/json" \
  -d '{"name": "Pizza Place", "address": "123 Main St"}'

# Создать блюдо
curl -X POST http://localhost/api/restaurants/1/dishes \
  -H "Content-Type: application/json" \
  -d '{"name": "Margherita", "description": "Classic pizza", "price": 12.99}'
```

### Оставление отзыва:
```bash
curl -X POST http://localhost/api/restaurants/1/dishes/1/reviews \
  -H "Content-Type: application/json" \
  -d '{"order_id": 1, "rating": 5, "comment": "Great pizza!"}'
```

### Получение аналитики:
```bash
# Общая аналитика ресторана
curl http://localhost/api/restaurants/1/analytics

# Топ блюд
curl http://localhost/api/restaurants/1/top-dishes
```

---

